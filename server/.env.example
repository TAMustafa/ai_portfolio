# Backend runtime environment variables for Cloud Run
# Never commit real secrets to version control.

# Required: OpenAI API key used by /api/chat
OPENAI_API_KEY="sk-..."

# Optional: Cloud Run sets PORT automatically; keep this commented locally
# PORT=8080
